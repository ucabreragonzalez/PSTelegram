[CmdletBinding()]
Param(
	[Parameter(Mandatory = $false)]
	[switch]
	$Deploy,
	[Parameter(Mandatory = $false)]
	[switch]
	$SkipTests
)
## Do some magic!!...
## Well for now you need to write something in here
